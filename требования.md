# Обязательные требования
Семестровая работа – сайт, написанный на Java Spring Boot с использованием Spring MVC Framework. Это должно быть MPA.
Сайт должен иметь удобный интерфейс. Необходимо использовать HTML5, CSS3. CSS-препроцессоры не запрещаются, но нужно знать их основные принципы.
Если используете CSS-фреймворки (Bootstrap и проч.), должны уметь пояснить, что за классы используете, особенно классы сетки. Необходимо корректно работать с формами: проверка данных, защита от повторной отправки там, где это нужно. 
Нужно использовать любой шаблонизатор, можно JSP. В коде видов нельзя напрямую вызывать Java-код.
Должна быть развитая система шаблонов (свои теги или наследование шаблонов, разделение видов на несколько частей).
Сайт должен иметь аутентификацию, авторизацию, регистрацию.
Сайт должен быть защищён от атак, которые обсуждались в течение всего курса предмета(csrf, xss, sql инъекции).
Сайт должен хранить данные в реляционной СУБД PostgreSQL.
Для работы с СУБД необходимо использовать и Spring Data JPA, и JPA. То есть должны быть как и репозитории от Spring, так и самостоятельно написанные запросы с помощью JPQL/CriteriaBuilder.
Система должна работать, как минимум с шестью JPA сущностями. Должны быть связи M2M, O2M.
Как минимум, для одной из сущностей должен быть реализован полностью CRUD: не обязательно в виде админки, но должны быть некие экшены контроллеров, которые реализуют CRUD-действия.
Система должна быть построена по модели MVC, и следует придерживаться логики Controller->Service->Repository(DAO) и т.д.
Необходимо придерживаться принципов SOLID. Весь код должен быть корректно поделен на пакеты. Рекомендуется использовать DTO-, Request-, Forms-классы, где это уменьшает связность, абстрагирует архитектуру.
Проект должен быть основан на Spring Boot: нужно использовать стартеры, настройки и запускающий класс.
Все реализации требований должны вписываться в бизнес-логику и выполнение соответствующего кода должно быть возможно из-за действий пользователя.
Необходимо использовать Docker (сервер приложений и сервер СУБД в отдельных контейнерах).

# Требования к наполнению, наличию реализаций
● Необходимо применить Javascript. Нужно использовать AJAX.
● Все исключения, проблемы должны корректно обрабатываться: обычные экшены – приводить к отдачи самостоятельно оформленной веб-страницы, а AJAX-запросы получить JSON-ответ с информацией о проблеме.
Не должно быть дефолтных страниц с ошибками от сервера приложений или Spring Boot.
● Использование стороннего API, не используя специальную библиотеку для этого API (SDK), а самостоятельное описание запросов на уровне HTTP (например, библиотека okhttp).
Примеры: вход или восстановление пароля через отправку смс, использование курсов валют для пересчёта стоимости корзины.
Наличие простого информера для сайта типа вывода погоды не подходит!
● Использование Spring Security. То есть полноценная работа с пользователем: закрытая часть сайта, аутентификация, регистрация.
● Организация REST API хотя бы для одной сущности. Подключение генерации openAPI. А также написание тестов на выбор через http-файлы Idea, через Postman или аналоги.
● Использование конвертеров, в том числе и своих.
● Логирование возникающих исключений.
● Наличие 2 нестандартных методов для репозиториев Spring:самостоятельно написанных методов с использованием @Query, CriteriaBuiler, но не повторяющих функционал методов, описываемых в Spring Data JPA.
● Один запрос с подзапросом.
● Наличие кэширования с помощью Redis или аналогов.

# Идея сайта и как будут реализованы требования в моем случае
«Культурный навигатор города»
Сайт для поиска культурного досуга: выставки, спектакли, концерты, лекции, мастер-классы, музеи, а также готовые тематические маршруты по интересам.
Пользователь получает:
персональные рекомендации;
готовые маршруты “на вечер”, “на выходные”, “для пары”, “для студента”;
подборки по жанру, бюджету, времени и району;
возможность сохранять места и события в избранное;
отзывы и оценки;
публикацию собственных маршрутов

1. Spring MVC Framework и MPA

главная страница;
каталог событий;
страница события;
каталог мест;
страница маршрута;
личный кабинет;
страницы администрирования/модерации.
2. Удобный интерфейс
Будет использоваться bootstrap

Можно сделать:
понятное меню;
фильтры слева;
карточки мероприятий;
адаптивную верстку;
удобные формы отзывов, добавления маршрутов, редактирования профиля.
3. HTML5, CSS3, формы

регистрация;
вход;
редактирование профиля;
создание маршрута;
добавление отзыва;
фильтры поиска;
добавление мероприятия в избранное/план.

Можно сделать:

серверную валидацию;
клиентскую проверку обязательных полей;
защиту от повторной отправки формы отзыва или создания маршрута.
4. Шаблонизатор

Будет использован freemarker:

общий layout;
фрагменты header/footer/sidebar;
отдельные шаблоны карточек;
переиспользуемые блоки пагинации и уведомлений.

5. Аутентификация, авторизация, регистрация

Роли:

Гость — смотрит каталог;
Пользователь — оставляет отзывы, делает маршруты, сохраняет избранное;
Администратор — управляет категориями, площадками, событиями, отзывами и маршрутами
6. Защита сайта

CSRF-защита; XSS, SQL инъекции
экранирование пользовательского контента;
валидация входных данных;
ограничение доступа по ролям;
защита от повторной отправки;
корректная обработка ошибок;
хеширование паролей;
проверка прав на редактирование только своих маршрутов и отзывов.
7. Реляционная БД и минимум 6 JPA-сущностей

User
Role
Event
Venue
Category
Route
Review
Favorite
CityDistrict
RouteItem
8. Связи M2M и O2M
User — Role: M2M
Event — Category: M2M
Venue — Event: O2M
User — Review: O2M
Route — RouteItem: O2M
User — Favorite: O2M
9. Полный CRUD

полный CRUD для сущности Route:

create — создать маршрут;
read — посмотреть маршрут;
update — редактировать маршрут;
delete — удалить маршрут.

10. Controller → Service → Repository

EventController
EventService
EventRepository
RouteController
RouteService
RouteRepository
и так далее.

11. Javascript и AJAX

динамическая фильтрация событий без полной перезагрузки;
добавление в избранное;
подгрузка комментариев;
лайк/оценка;
проверка уникальности username/email при регистрации;
12. Обработка ошибок
    для обычных страниц — красивые error pages: 404, 403, 500;
    для AJAX — JSON вида:
    {
    "status": "error",
    "message": "Маршрут не найден"
    }
13. Внешнее API без SDK

У Яндекса есть Geocoder API, который преобразует адреса в координаты и наоборот. Запрос выполняется по HTTP, например:

https://geocode-maps.yandex.ru/v1/?apikey=YOUR_API_KEY&geocode=Казань,+Кремлевская,+18&lang=ru_RU&format=json

В документации Яндекса указано, что Geocoder API умеет прямое и обратное геокодирование, а координаты в ответе приходят в формате longitude latitude.

Также у Яндекса есть Retrieving Route Details API — HTTP API для построения маршрутов между точками

использовать API карт/геокодинга Yandex через обычные HTTP-запросы;
например, по адресу площадки получать координаты;
строить примерную длину маршрута;
показывать расстояние между точками маршрута.

14. REST API + OpenAPI

Можно сделать REST API для сущности  Route:

GET /api/routes
POST /api/routes
PUT /api/routes/{id}
DELETE /api/routes/{id}

И подключить генерацию OpenAPI/Swagger.

15. Конвертеры

Например:

String -> Category
String -> LocalDate
String -> Venue
свой конвертер query-параметра фильтра в enum/DTO.
16. Логирование исключений

Подходит глобальный обработчик + логирование:

ошибки валидации;
попытки доступа без прав;
ошибки внешнего API;
ошибки БД.
17. Два нестандартных метода репозитория

Например:

поиск событий, где средняя оценка выше заданной;
подбор маршрутов по диапазону бюджета и длительности;

Один из них — через @Query, другой — через CriteriaBuilder.

18. Подзапрос

Например:

выбрать события, у которых рейтинг выше среднего по своей категории;
выбрать площадки, где число мероприятий больше среднего по городу;
выбрать маршруты, которые содержат хотя бы одно событие с высокой оценкой.
19. Кэширование

Через Redis можно кэшировать:

популярные события;
категории;
главную страницу;
топ маршрутов;
карточки площадок.
20. Docker

контейнер приложения;
контейнер PostgreSQL/MySQL;
контейнер Redis.